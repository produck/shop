import * as Registry from './Registry.mjs';

const assert = (source, target) => {

};

export function ToOne(source, target, as) {
	assert(source, target);
}

export function ToMany(source, target, as) {
	assert(source, target);
}

export async function query(filter) {

}
